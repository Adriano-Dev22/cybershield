version: '3.8'

services:
  cybershield:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - .:/app
    environment:
      - SECRET_KEY=supersecretkeychangeme123!
    restart: unless-stopped